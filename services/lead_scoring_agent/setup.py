from setuptools import setup, find_packages

setup(
    name="lead_scoring_agent",
    version="0.1.0",
    packages=find_packages(),
    description="A lead scoring agent for the multi-agent AI platform.",
    author="Jules",
    author_email="",
    url="",
)
